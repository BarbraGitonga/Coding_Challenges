#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <stdlib.h>

int part_number(char){
    int dx[] = {-1, 1, 0, 0}; // x coordinates
    int dy[] = {0, 0, -1, 1}; // y coordinates
    for ()
}
int line_divider(const char*filename) {
    char line[131];
    int sum = 0;
    while (fgets(line, sizeof line, filename) != NULL){
        
    }
    
}